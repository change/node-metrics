machine:
  node:
    version: 0.12.5

deployment:
  stable:
    branch: master
    commands:
      - '[[ ! -s "$(git rev-parse --git-dir)/shallow" ]] || git fetch --unshallow'
      - git push origin $CIRCLE_SHA1:stable

general:
  branches:
    ignore:
      - stable
